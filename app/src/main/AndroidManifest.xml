<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.elevator_app">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".SpecificLineActivity"
            android:label="@string/title_activity_specific_line"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity
            android:name=".AllLinesActivity"
            android:label="@string/title_activity_all_lines"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".AddFavoriteActivity"
            android:label="@string/title_activity_add_favorite"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".DisplayAlertActivity"
            android:label="@string/title_activity_display_alert"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".MainActivity"
            android:label="CTA Elevator Alerts"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>